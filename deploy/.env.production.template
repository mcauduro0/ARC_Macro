# ============================================================
# BRLUSD Dashboard — DigitalOcean Environment Variables
# ============================================================
# Copy this file to /opt/brlusd-dashboard/.env and fill in values
# ============================================================

# ── Server ────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000

# ── Database (MySQL local) ────────────────────────────────────
DATABASE_URL=mysql://brlusd:YOUR_PASSWORD_HERE@localhost:3306/brlusd_dashboard

# ── JWT Secret (generate: openssl rand -base64 32) ────────────
JWT_SECRET=CHANGE_ME_GENERATE_A_RANDOM_STRING

# ── Owner Identity ────────────────────────────────────────────
OWNER_OPEN_ID=do-owner
OWNER_NAME=Your Name

# ── Email Notifications (SMTP) ────────────────────────────────
# Gmail: smtp.gmail.com:587 (use App Password, not regular password)
# Outlook: smtp.office365.com:587
# Custom: your SMTP server
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
NOTIFY_EMAIL=your-email@gmail.com

# ── Data Source API Keys ──────────────────────────────────────
# FRED (Federal Reserve Economic Data)
# Get free key: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_key

# Financial Modeling Prep
# Get key: https://financialmodelingprep.com/developer
FMP_API_KEY=your_fmp_key

# Trading Economics
# Get key: https://tradingeconomics.com/api
TE_API_KEY=your_te_key

# ANBIMA (Brazilian bond data)
# Get credentials: https://developers.anbima.com.br/
ANBIMA_CLIENT_ID=your_anbima_client_id
ANBIMA_CLIENT_SECRET=your_anbima_client_secret

# Polygon.io (Market data)
# Get key: https://polygon.io/
POLYGON_API_KEY=your_polygon_key

# ── Manus Compatibility (leave empty for DO deployment) ───────
# These are needed by _core modules but won't be used
VITE_APP_ID=do-standalone
VITE_APP_TITLE=BRLUSD Dashboard
VITE_OAUTH_PORTAL_URL=
OAUTH_SERVER_URL=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
